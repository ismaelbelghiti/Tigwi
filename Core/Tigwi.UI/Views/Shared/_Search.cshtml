@model string
           
@using (Html.BeginForm("Search", "Account", FormMethod.Post, new { @class = "navbar-search" }))
{
    @Html.TextBox("searchString", Model, new { @class = "search-query", placeHolder = "Search for an account" });
    <input type="submit" style="display: none;"/>
}