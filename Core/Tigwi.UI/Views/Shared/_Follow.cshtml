@model Tigwi.UI.Models.IListModel

<div class="row-fluid">

@using (Html.BeginForm("Follow", "List", new { id = Model.Id }))
{
    <button type="submit" class="btn btn-primary" id="followButton">Follow !</button>
}
</div>
