@model Tigwi.UI.Models.IListModel

<div class="row-fluid">

@using (Html.BeginForm("FollowList", "List", new { id = Model.Id }))
{
    <button type="submit" class="btn btn-primary" id="followButton-@Model.Id">Follow !</button>
}
</div>