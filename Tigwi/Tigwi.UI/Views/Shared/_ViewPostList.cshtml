@using Tigwi.UI.Models
@model ICollection<IPostModel>
           
@{int i=0;}
@foreach( var post in Model.Reverse())
{
    var toSend=new PostViewModel { Post = post, Color = i%2};
    i++;
    @Html.Partial("_ViewPost", toSend);
}
